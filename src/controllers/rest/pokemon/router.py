"""
Pokemon feature removed.

This module previously contained REST endpoints for Pokemon CRUD operations.
The feature has been removed and the module intentionally left as a placeholder
to avoid import errors while refactoring other parts of the codebase.
"""

from fastapi import APIRouter

router = APIRouter()
