apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: azure-keyvault-homunculy
spec:
  parameters:
    clientID: "<YOUR_DEV_AZURE_CLIENT_ID>"
    keyvaultName: "kv-homunculy-dev"
    tenantId: "<YOUR_TENANT_ID>"
